name: Private STM32 model tests

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feat/**'

permissions:
  contents: read

concurrency:
  group: private-model-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  stm32-models:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Require private Infrastructure access
        env:
          INFRASTRUCTURE_DEPLOY_KEY: ${{ secrets.RENODE_INFRASTRUCTURE_DEPLOY_KEY }}
        run: |
          if [ -z "$INFRASTRUCTURE_DEPLOY_KEY" ]; then
            echo '::error::Set repository secret RENODE_INFRASTRUCTURE_DEPLOY_KEY to the read-only deploy key for CrispStrobe/renode-infrastructure-spike-prime.'
            exit 1
          fi

      - name: Check out pinned source and submodules
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
          persist-credentials: false
          submodules: recursive
          ssh-key: ${{ secrets.RENODE_INFRASTRUCTURE_DEPLOY_KEY }}

      - name: Set up .NET 8
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: 8.0.x

      - name: Verify the private submodule revision
        shell: bash
        run: |
          expected=$(git ls-tree HEAD src/Infrastructure | awk '{print $3}')
          actual=$(git -C src/Infrastructure rev-parse HEAD)
          if [ "$actual" != "$expected" ]; then
            echo "::error::Infrastructure checkout mismatch: expected $expected, got $actual"
            exit 1
          fi

      - name: Build native translator libraries
        run: ./build.sh --net --no-gui --external-lib-only

      - name: Run focused STM32 model tests
        shell: bash
        run: |
          project=src/Infrastructure/src/Emulator/Peripherals/Test/PeripheralsTests/PeripheralsTests_NET.csproj
          common=(-p:TargetFrameworks=net8.0 -p:GUI_DISABLED=true -p:CurrentPlatform=Linux)
          dotnet restore "$project" "${common[@]}"
          dotnet test "$project" --configuration Release --no-restore \
            "${common[@]}" \
            --filter 'FullyQualifiedName~STM32DMATests|FullyQualifiedName~STM32SPITests|FullyQualifiedName~STM32_UARTTests' \
            --logger 'console;verbosity=normal'
